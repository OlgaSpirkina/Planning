<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <!-- CSS & Font -->
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans&display=swap" rel="stylesheet">
    <!-- JS -->
  <script src="/js/hello.js" type="text/javascript" defer></script>
  <script src="/js/header.js" type="text/javascript" defer></script>
  <title>Monthly Planning</title>
</head>
<body class="body">
  {{>header}}

  <section class="container-fluid">
    <div class="row">
      <div class="col-12 d-flex flex-column">
        <h3 class="text-center">{{#each objectForEachMonth}}<span id="current-month">{{@key}}</span>{{/each}} {{currentYear}}</h3>
        <div class="d-flex flex-wrap justify-content-evenly align-items-center">
          {{#each objectForEachMonth}}
          {{#each this}}
          <div class="parent-calendar-event">
            <h5 class="formated-day">{{@key}}</h5>
              <div>
                <div style="border: 1px solid brown; margin: .5rem">
                  {{#each this}}
                  <div class="d-flex justify-content-between align-items-center parent-data">
                  <p>{{this.time}}</p>
                  <p>{{this.name}} {{this.fname}}</p>
                  <p>{{this.company}}</p>
                  <p>{{this.total_classes}}</p>
                  <p hidden>{{this.trainer_id}}</p>
                  <button class="change-trainers-planning" value="{{this.trainer_id}}"><a href="updateplanning/{{this.fname}}{{this.name}}">modifier<a/></button>
                  </div>
                  {{/each}}
                </div>
              </div>
          </div>
          {{/each}}
          {{/each}}
        </div>
      </div>
    </div>
  </section>

  <section class="container-fluid">
    <div class="row">
      <div class="d-flex flex-column justify-content-center align-items-center">
        <div>
          <button class="month-btn previous-btn"><a id="previous-month">Mois Précedent</a></button>
          <button class="month-btn next-btn"><a id="next-month">Mois Suivant</a></button>
        </div>
        </div>
      </div>
    </div>
  </section>
</body>
</html>
