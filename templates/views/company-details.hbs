<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
    <!-- CSS & Font -->
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Open+Sans&display=swap" rel="stylesheet">
    <!-- JS -->
  <script src="/js/header.js" type="text/javascript" defer></script>
  <script src="/js/company-details.js" type="text/javascript" defer></script>
  <title>Company details</title>
</head>
<body class="body">
  {{>header}}
  <section class="container-fluid">
    <p>Les professeurs qui travaillent à
      {{#each result}}{{#if @first}}{{#each this}}{{#if @first}}
        {{this.company}}
      {{/if}}{{/each}}{{/if}}{{/each}}
    </p>
    {{#each result}}
      <p>{{@key}}</p>
    {{/each}}
  </section>
  <section class="container-fluid">
    <div class="row">
      <div class="col-12">
        <div id="company-calendar" class="d-flex flex-wrap">
          {{#each companyCalendar}}
          <div class="d-flex flex-column justify-content-center align-items-center">
          <h3>{{@key}}</h3>
            <div class="company-each-month d-flex flex-wrap justify-content-evenly align-items-center">
                {{#each this}}
                <div class="days">
                  <p>{{@key}}</p>
                  {{#each this}}
                  <p>{{this.time}}</p>
                  {{/each}}
                </div>
                {{/each}}
            </div>
          </div>
          {{/each}}
        </div>
      </div>
    </div>
  </section>
  {{>footer}}
</body>
</html>
